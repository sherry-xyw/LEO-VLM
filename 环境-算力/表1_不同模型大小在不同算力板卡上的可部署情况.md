# 表 1：不同模型大小在不同算力板卡上的可部署情况

## 1. 目标、参数与配置

### 1.1 部署目标

视觉—语言模型（Vision-Language Model, VLM）在给定板卡、量化配置和参数取值下，可在单板、无中央处理器（Central Processing Unit, CPU）或 NVMe（Non-Volatile Memory Express） 固态存储卸载、无多卡并行的情况下稳定完成一次推理任务。

### 1.2 参数与定义

所有内存统一使用 GiB（gibibyte）：

$$
1\ \mathrm{GiB}=2^{30}\ \mathrm{Byte}.
$$

部分名词解释：B 表示十进制十亿，即 $1\ \mathrm{B}=10^9$ parameters。Byte 表示字节，$1\ \mathrm{Byte}=8$ bit。W4/W8 分别表示 4-bit/8-bit 权重量化；BF16 表示 Brain Floating Point 16-bit；TFLOPs 表示每秒万亿次浮点运算；FLOPs 表示浮点运算次数；TTFT（time to first token）表示首个 token 的生成时延；GPU、NPU 和 FPGA 分别表示图形处理器、神经网络处理器和现场可编程门阵列。

下列两个表分别列出内存可部署性计算中需要统一预设或记录的主要符号。

模型相关参数：

| 参数                                   | 含义                                                     | 取值或说明（以 Qwen3-VL-4B-Instruct 为例）                                           |
| ------------------------------------ | ------------------------------------------------------ | -------------------------------------------------------------------------- |
| $\mathcal{C}$                        | 推理参数集合                                                 | $\{B,N_{\mathrm{text}},N_{\mathrm{vis}},N_{\mathrm{out}}\}$                |
| $\mathcal{M}$                        | 模型配置集合                                                 | 记录参数量、层数、head 配置与量化精度                                                      |
| $P_{\mathrm{LLM}}$                   | 语言主干参数量                                                | 约 $4.022$B                                                                 |
| $P_{\mathrm{vis}}$                   | 视觉编码器参数量                                               |                                                                            |
| $P_{\mathrm{proj}}$                  | 跨模态投影器参数量                                              |                                                                            |
| $P_{\mathrm{vis}}+P_{\mathrm{proj}}$ | 视觉编码器与投影器参数量总和                                         | 约 $0.415$B                                                                 |
| $P_{\mathrm{total}}$                 | 完整 VLM 总参数量                                            | 约 $4.438$B                                                                 |
| $L$                                  | 语言模型 Transformer 层数                                    | 36                                                                         |
| $\ell$                               | Transformer 层索引                                        | $\ell\in\{1,\ldots,L\}$                                                    |
| $H$                                  | 语言模型 hidden size                                       | 2560                                                                       |
| $n_{\mathrm{q}}$                     | 每层 query heads 数                                       | 32                                                                         |
| $n_{\mathrm{kv}}$                    | 每层 KV heads 数                                          | 8                                                                          |
| $n_{\mathrm{kv},\ell}$               | 第 $\ell$ 层 KV heads 数                                  | 各层相同时为 $n_{\mathrm{kv}}$                                                   |
| $d_h$                                | 每个 attention head 的维度                                  | 128                                                                        |
| $d_{h,\ell}$                         | 第 $\ell$ 层 attention head 维度                           | 各层相同时为 $d_h$                                                               |
| $D_Q$                                | attention 中 query 的总投影维度，$D_Q=n_{\mathrm{q}}d_h$       | $32\times128=4096$                                                         |
| $K^{(\ell)}$                         | 第 $\ell$ 层 Key cache 张量                                | 维度为 $B\times T_{\mathrm{cap}}\times n_{\mathrm{kv},\ell}\times d_{h,\ell}$ |
| $V^{(\ell)}$                         | 第 $\ell$ 层 Value cache 张量                              | 维度同 $K^{(\ell)}$                                                           |
| $B$                                  | batch size                                             | 1                                                                          |
| $N_{\mathrm{text}}$                  | 文本输入 token 数                                           | 1024                                                                       |
| $N_{\mathrm{vis}}$                   | 实际输入语言模型的视觉 token 数                                    | 按模型 processor 实际记录；本文取 1024                                                |
| $N_{\mathrm{out}}$                   | 最大生成 token 数                                           | 1024                                                                       |
| $T_{\mathrm{in}}$                    | 进入语言模型的文本 token 与视觉 token 总数                           | $N_{\mathrm{text}}+N_{\mathrm{vis}}=2048$                                  |
| $T_{\mathrm{cap}}$                   | KV cache 需要容纳的最大 token 数                               | $T_{\mathrm{in}}+N_{\mathrm{out}}=3072$                                    |
| $T_{\mathrm{model,max}}$             | 模型架构或配置文件允许的最大上下文长度                                    | 按模型配置记录                                                                    |
| $b^{\mathrm{eff}}$                   | 模块的实际有效存储位宽（bit/parameter）                             | 语言主干 W4 为 $4.25$ bit，W8 为 $8.5$ bit；视觉编码器与投影器为 BF16（16 bit）                |
| $b_{\mathrm{LLM}}^{\mathrm{eff}}$    | 语言主干的实际有效存储位宽                                          | W4：$4.25$ bit；W8：$8.5$ bit                                                 |
| $b_{\mathrm{vis}}^{\mathrm{eff}}$    | 视觉编码器的实际有效存储位宽                                         | BF16：16 bit                                                                |
| $b_{\mathrm{proj}}^{\mathrm{eff}}$   | 跨模态投影器的实际有效存储位宽                                        | BF16：16 bit                                                                |
| $b_{\mathrm{KV}}$                    | Key–Value（KV）cache 每个元素的存储位宽（bit）                      | BF16：16 bit；8-bit 浮点或 8-bit 整数格式：8 bit                                     |
| $s_{\mathrm{KV}}$                    | KV cache 每元素的存储字节数，$s_{\mathrm{KV}}=b_{\mathrm{KV}}/8$ | BF16：2 Byte                                                                |
| $M_{\mathrm{LLM}}$                   | 语言主干权重内存                                               | W4 示例：$1.990$ GiB                                                          |
| $M_{\mathrm{vis+proj}}$              | 视觉编码器与投影器权重内存                                          | BF16 示例：$0.774$ GiB                                                        |
| $M_{\mathrm{weight}}$                | 完整 VLM 权重内存                                            | W4 示例：$2.764$ GiB                                                          |
| $M_{\mathrm{KV}}$                    | 所有层 KV cache 内存                                        | 示例：$0.422$ GiB                                                             |
| $c_{\mathrm{KV}}$                    | 每 token 的 KV cache 内存成本                                | 示例：$144$ KiB/token                                                         |
| $M_{\mathrm{base}}$                  | 模型侧基础内存，不含板卡相关辅助内存                                     | $M_{\mathrm{weight}}+M_{\mathrm{KV}}=3.186$ GiB                            |
| $M_{\mathrm{KV,self}}$               | decoder self-attention 的 KV cache 内存                   | 交叉注意力架构时单独记录                                                               |
| $M_{\mathrm{KV,cross}}$              | cross-attention 中视觉 Key/Value 的缓存内存                    | 交叉注意力架构时单独记录                                                               |
| $M_{\mathrm{vision\ memory}}$        | 视觉编码器输出特征及其缓存占用                                        | 交叉注意力架构时单独记录                                                               |
| $M_{\mathrm{total}}$                 | 交叉注意力架构下的总缓存相关内存                                       | $M_{\mathrm{KV,self}}+M_{\mathrm{vision\ memory}}+M_{\mathrm{KV,cross}}$   |
| $M_{\mathrm{aux}}$                   | 模型执行辅助内存，不含操作系统、驱动和其他已扣除的保留内存                          | 分析性假设：1B–2B：$0.5$ GiB；3B–4B：$1.0$ GiB；7B–8B：$1.5$ GiB；32B：$3.0$ GiB        |
| $M_{\mathrm{req}}$                   | 任务峰值内存需求                                               | $M_{\mathrm{base}}+M_{\mathrm{aux}}$                                       |

板卡相关参数：

| 参数                       | 含义                         | 取值或说明（以 Qwen3-VL-4B-Instruct 为例）                                                        |
| ------------------------ | -------------------------- | --------------------------------------------------------------------------------------- |
| $\mathcal{H}$            | 板卡硬件配置集合                   | 记录内存、算力、带宽和功耗                                                                           |
| $M_{\mathrm{phys}}$      | 板卡物理内存容量                   | 按板卡规格记录                                                                                 |
| $M_{\mathrm{usable}}$    | 板卡可实际分配给推理进程的内存            | 示例板卡 A/B/C：$6.8$ / $13.6$ / $27.2$ GiB                                                  |
| $M_{\mathrm{OS/driver}}$ | 操作系统、驱动和基础运行环境占用的内存        | 按设定记录                                                                                   |
| $M_{\mathrm{reserved}}$  | 系统或其他计划服务保留、不可分配给推理进程的内存   | 按设定记录                                                                                   |
| $T_{\mathrm{cap,max}}$   | 由剩余内存约束得到的理论最大缓存长度         | 由 $M_{\mathrm{usable}}$、$M_{\mathrm{weight}}$、$M_{\mathrm{aux}}$ 和 $c_{\mathrm{KV}}$ 计算 |
| $T_{\max}$               | 最终可用最大上下文长度                | 内存上限与模型架构上限二者中的较小值                                                                      |
| $N_{\mathrm{out,max}}$   | 给定输入和内存约束下可支持的最大输出 token 数 | $T_{\max}-N_{\mathrm{text}}-N_{\mathrm{vis}}$                                           |
| $\Delta M$               | 内存裕量                       | $M_{\mathrm{usable}}-M_{\mathrm{base}}-M_{\mathrm{aux}}$                                |
| $\Delta M_{\mathrm{W8}}$ | W8 配置下的内存裕量                | 示例板卡 A：$0.624$ GiB                                                                      |
| $C_{\mathrm{peak}}$      | 板卡理论峰值计算能力                 | 按板卡规格记录                                                                                 |
| $B_{\mathrm{mem}}$       | 板卡理论内存带宽                   | 按板卡规格记录                                                                                 |
| $P_{\mathrm{max}}$       | 板卡最大功耗                     | 按板卡规格记录                                                                                 |

推理参数集合记为 $\mathcal{C}$，定义为：

$$
\mathcal{C}=\{B,N_{\mathrm{text}},N_{\mathrm{vis}},N_{\mathrm{out}}\}.
$$

总输入长度 $T_{\mathrm{in}}$ 表示进入语言模型的文本 token 与视觉 token 总数，定义为：

$$
T_{\mathrm{in}}=N_{\mathrm{text}}+N_{\mathrm{vis}},
$$

缓存长度 $T_{\mathrm{cap}}$ 表示推理过程中 KV cache 需要容纳的最大 token 数，定义为：

$$
T_{\mathrm{cap}}=T_{\mathrm{in}}+N_{\mathrm{out}}.
$$

### 1.3 输入配置

#### 1.3.1 模型配置

用 $\mathcal{M}$ 表示模型配置集合。其中，$b_{\mathrm{LLM}}^{\mathrm{eff}}$、$b_{\mathrm{vis}}^{\mathrm{eff}}$ 与 $b_{\mathrm{proj}}^{\mathrm{eff}}$ 分别表示语言主干、视觉编码器和跨模态投影器的实际有效存储位宽，$b_{\mathrm{KV}}$ 表示 KV cache 元素位宽。对每个模型记录以下信息：

$$
\mathcal{M}=\{P_{\mathrm{LLM}},P_{\mathrm{vis}},P_{\mathrm{proj}},L,H,n_{\mathrm{q}},n_{\mathrm{kv}},d_h,b_{\mathrm{LLM}}^{\mathrm{eff}},b_{\mathrm{vis}}^{\mathrm{eff}},b_{\mathrm{proj}}^{\mathrm{eff}},b_{\mathrm{KV}}\}.
$$

- 参数量覆盖完整 VLM，包括语言主干、视觉编码器、跨模态投影器（projector）、任务头和已加载的适配模块（Adapter）；
- 对混合专家模型（Mixture-of-Experts, MoE），内存按总参数量计算，计算量按活跃参数量计算；
- 权重量化精度与 KV cache 精度分别记录。

#### 1.3.2 板卡配置

用 $\mathcal{H}$ 表示板卡硬件配置集合，其中 $M_{\mathrm{phys}}$ 为物理内存容量，$M_{\mathrm{usable}}$ 为可分配给推理进程的有效内存，$C_{\mathrm{peak}}$ 为理论峰值计算能力，$B_{\mathrm{mem}}$ 为理论内存带宽，$P_{\mathrm{max}}$ 为板卡最大功耗。对每个板卡记录：

$$
\mathcal{H}=\{M_{\mathrm{phys}},M_{\mathrm{usable}},C_{\mathrm{peak}},B_{\mathrm{mem}},P_{\mathrm{max}}\}.
$$

其中，$M_{\mathrm{OS/driver}}$ 为操作系统、驱动和基础运行环境占用的内存，$M_{\mathrm{reserved}}$ 为已被系统或其他计划服务保留、不可分配给推理进程的内存。$M_{\mathrm{usable}}$ 根据实际环境获得：

$$
M_{\mathrm{usable}}=M_{\mathrm{phys}}-M_{\mathrm{OS/driver}}-M_{\mathrm{reserved}}.
$$

对于 Jetson 等统一内存板卡，还应扣除操作系统、CPU 进程和图像预处理等共享内存占用。

---
## 2. 计算过程
### 2.1 步骤一：内存可部署性计算

#### 2.1.1 静态权重内存

令 $M_{\mathrm{weight}}$ 表示完整 VLM 权重占用的内存，则：

$$
M_{\mathrm{weight}}
=
\frac{
P_{\mathrm{LLM}}b_{\mathrm{LLM}}^{\mathrm{eff}}
+
P_{\mathrm{vis}}b_{\mathrm{vis}}^{\mathrm{eff}}
+
P_{\mathrm{proj}}b_{\mathrm{proj}}^{\mathrm{eff}}
}{8\cdot 2^{30}}
\quad [\mathrm{GiB}].
$$

若还存在任务头、低秩适配（Low-Rank Adaptation, LoRA）或其他模块，应将相应项继续加入分子。

对于模块量化方式一致的情形，$P_{\mathrm{total}}$ 表示完整 VLM 总参数量，$b^{\mathrm{eff}}$ 表示所有模块统一量化时的平均有效位宽，可使用近似式：

$$
M_{\mathrm{weight}}\approx \frac{P_{\mathrm{total}}b^{\mathrm{eff}}}{8\cdot 2^{30}}.
$$

其中分母中的 8 用于将 bit 换算为 Byte；该近似忽略 scale、zero-point、padding 等细粒度存储开销。

#### 2.1.2 KV cache 内存

对于“视觉 token 与文本 token 拼接后进入解码器（decoder）”的 VLM，Transformer 第 $\ell$ 层需要保存 Key 张量 $K^{(\ell)}$ 与 Value 张量 $V^{(\ell)}$。其中，$\ell\in\{1,\ldots,L\}$ 为 Transformer 层索引；$n_{\mathrm{kv},\ell}$ 与 $d_{h,\ell}$ 分别为第 $\ell$ 层的 KV heads 数和单个 attention head 维度：

$$
K^{(\ell)},V^{(\ell)}\in \mathbb{R}^{B\times T_{\mathrm{cap}}\times n_{\mathrm{kv},\ell}\times d_{h,\ell}}.
$$

令 $M_{\mathrm{KV}}$ 表示所有层的 Key–Value（KV）cache 内存。下式中的 $\sum_{\ell=1}^{L}$ 表示从第 1 层到第 $L$ 层累加：

$$
M_{\mathrm{KV}}
=
\frac{
B T_{\mathrm{cap}}
\sum_{\ell=1}^{L}
\left(2n_{\mathrm{kv},\ell}d_{h,\ell}s_{\mathrm{KV}}\right)
}{2^{30}}
\quad [\mathrm{GiB}].
$$

若各层结构相同，则化简为：

$$
M_{\mathrm{KV}}
=
\frac{
2BT_{\mathrm{cap}}Ln_{\mathrm{kv}}d_hs_{\mathrm{KV}}
}{2^{30}}.
$$

定义每 token 的 KV cache 成本：

$$
c_{\mathrm{KV}}
=
\frac{2BLn_{\mathrm{kv}}d_hs_{\mathrm{KV}}}{2^{30}}
\quad [\mathrm{GiB/token}],
$$

则：

$$
M_{\mathrm{KV}}=T_{\mathrm{cap}}c_{\mathrm{KV}}.
$$

注意：若 VLM 通过交叉注意力（cross-attention）将视觉特征作为独立编码器记忆（encoder memory）输入解码器，则视觉 token 不应直接加入解码器自注意力（self-attention）的 $T_{\mathrm{cap}}$。此时，$M_{\mathrm{total}}$ 为总内存，$M_{\mathrm{KV,self}}$ 为 decoder self-attention 的 KV cache 内存，$M_{\mathrm{vision\ memory}}$ 为视觉编码器输出特征及其缓存占用，$M_{\mathrm{KV,cross}}$ 为 cross-attention 中视觉 Key/Value 的缓存内存：

$$
M_{\mathrm{total}}=M_{\mathrm{KV,self}}+M_{\mathrm{vision\ memory}}+M_{\mathrm{KV,cross}}.
$$

#### 2.1.3 辅助内存

令 $M_{\mathrm{aux}}$ 表示模型执行过程中的辅助内存，包括视觉编码器激活、projector、attention/GEMM/量化 kernel 工作区、推理框架运行时、内存池和碎片余量。由于 $M_{\mathrm{usable}}$ 已扣除操作系统、驱动和其他保留内存，$M_{\mathrm{aux}}$ 不再重复计入这些部分。本文不再按板卡容量比例估算，而按模型规模给定固定取值。该项是用于理论初筛的分析性假设，并非特定推理后端、图像分辨率或运行时配置下的实测峰值内存：

| 模型规模 | $M_{\mathrm{aux}}$ | 适用说明 |
| --- | ---: | --- |
| 1B–2B VLM | $0.5$ GiB | 覆盖小型 VLM 的视觉编码器激活、projector、workspace、运行时内存池和少量碎片余量 |
| 3B–4B VLM | $1.0$ GiB | 覆盖视觉编码器激活、projector、workspace、运行时内存池和少量碎片余量 |
| 7B–8B VLM | $1.5$ GiB | 语言主干与量化 kernel 的工作区进一步增大 |
| 32B VLM | $3.0$ GiB | 面向大规模 W4 模型的保守静态估计 |

该项可随模型规模、推理后端、图像实际分辨率、动态 patch 策略、KV cache 优化、视觉模块量化、内存池和 batch 设置调整。

#### 2.1.4 峰值内存与容量判定

令 $M_{\mathrm{base}}$ 表示模型侧基础内存，$M_{\mathrm{req}}$ 表示任务峰值内存，$\Delta M$ 表示内存裕量：

$$
M_{\mathrm{base}}
=
M_{\mathrm{weight}}
+
M_{\mathrm{KV}}.
$$

$$
M_{\mathrm{req}}
=
M_{\mathrm{base}}
+
M_{\mathrm{aux}}.
$$

内存裕量定义为：

$$
\Delta M
=M_{\mathrm{usable}}-M_{\mathrm{req}}
=M_{\mathrm{usable}}-M_{\mathrm{base}}-M_{\mathrm{aux}}.
$$

内存可部署性二值判定 $\mathrm{Deployable}_{\mathrm{memory}}$ 定义为：

$$
\mathrm{Deployable}_{\mathrm{memory}}
=
\mathbb{I}(\Delta M\ge 0).
$$

其中 $\mathbb{I}(\cdot)$ 为示性函数：括号内条件成立时取 1，否则取 0；$\ge$ 表示大于或等于。

---

### 2.2 步骤二：最大上下文与输出长度

在给定板卡上，$T_{\mathrm{cap,max}}$ 表示由剩余内存约束得到的理论最大缓存长度。$\lfloor\cdot\rfloor$ 为下取整函数：

$$
T_{\mathrm{cap,max}}
=
\left\lfloor
\frac{
M_{\mathrm{usable}}
-M_{\mathrm{weight}}
-M_{\mathrm{aux}}
}{c_{\mathrm{KV}}}
\right\rfloor.
$$

最终可用最大上下文长度 $T_{\max}$ 应取内存上限与模型架构或配置文件允许的最大上下文长度 $T_{\mathrm{model,max}}$ 的较小值；$\min(\cdot,\cdot)$ 表示取较小值：

$$
T_{\max}=\min\left(T_{\mathrm{cap,max}},T_{\mathrm{model,max}}\right).
$$

给定输入后，$N_{\mathrm{out,max}}$ 表示可支持的最大输出 token 数：

$$
N_{\mathrm{out,max}}
=T_{\max}-N_{\mathrm{text}}-N_{\mathrm{vis}}.
$$

### 2.3 步骤三：计算量、时延与能耗筛选

#### 2.3.1 端到端时延分解

令 $T_{\mathrm{e2e}}$ 为端到端推理时延，$T_{\mathrm{preprocess}}$ 为图像与文本预处理时延，$T_{\mathrm{vision}}$ 为视觉编码器前向计算时延，$T_{\mathrm{prefill}}$ 为处理完整输入序列并建立初始 KV cache 的时延，$T_{\mathrm{decode}}$ 为逐 token 自回归生成时延：

$$
T_{\mathrm{e2e}}
=
T_{\mathrm{preprocess}}
+T_{\mathrm{vision}}
+T_{\mathrm{prefill}}
+T_{\mathrm{decode}}.
$$

其中，视觉编码和 prefill 通常更偏计算受限；自回归 decode 通常更易受权重与 KV cache 访问带宽限制。

#### 2.3.2 FLOPs 近似

FLOPs 表示浮点运算次数。对稠密 decoder-only 语言主干，$F_{\mathrm{vision}}$、$F_{\mathrm{prefill}}$、$F_{\mathrm{decode}}$ 和 $F_{\mathrm{task}}$ 分别表示视觉编码、prefill、decode 和单次任务的计算量。$T_{\mathrm{in}}$ 定义为：

$$
T_{\mathrm{in}}=N_{\mathrm{text}}+N_{\mathrm{vis}}.
$$

定义每层 attention 中 query 的总投影维度 $D_Q$：

$$
D_Q=n_{\mathrm{q}}d_h.
$$

prefill 的近似计算量为：

$$
F_{\mathrm{prefill}}
\approx
F_{\mathrm{vision}}
+2P_{\mathrm{LLM}}T_{\mathrm{in}}
+4LD_QT_{\mathrm{in}}^2.
$$

生成 $N_{\mathrm{out}}$ 个 token 的 decode 近似计算量为：

$$
F_{\mathrm{decode}}
\approx
2P_{\mathrm{LLM}}N_{\mathrm{out}}
+4LD_Q\left[
N_{\mathrm{out}}T_{\mathrm{in}}
+\frac{N_{\mathrm{out}}(N_{\mathrm{out}}-1)}{2}
\right].
$$

当 $D_Q=H$ 时，上式退化为常用的 $4LHT^2$ 形式，其中一般序列长度 $T$ 表示 attention 的输入 token 数。对于 Qwen3-VL-4B，$D_Q=4096\neq H=2560$，因此应使用 $D_Q$ 而非 $H$ 计算 attention 项。

总计算量：

$$
F_{\mathrm{task}}=F_{\mathrm{prefill}}+F_{\mathrm{decode}}.
$$

对于混合专家模型（MoE），以上公式中的 $P_{\mathrm{LLM}}$ 应替换为活跃参数量 $P_{\mathrm{active}}$，即一次前向推理中实际被路由并参与计算的参数量；$M_{\mathrm{weight}}$ 仍按总参数量计算。

#### 2.3.3 Roofline 下界与任务判定

令 $T_{\mathrm{infer}}$ 为理论推理时延下界，$F_{\mathrm{task}}$ 为任务总计算量，$\eta_C$ 为实际计算利用率，$C_{\mathrm{peak}}$ 为理论峰值计算能力，$D_{\mathrm{mem}}$ 为任务执行过程中的总访存量，$\eta_B$ 为实际内存带宽利用率，$B_{\mathrm{mem}}$ 为理论内存带宽：

$$
T_{\mathrm{infer}}
\gtrsim
\max\left(
\frac{F_{\mathrm{task}}}{\eta_C C_{\mathrm{peak}}},
\frac{D_{\mathrm{mem}}}{\eta_B B_{\mathrm{mem}}}
\right),
$$

其中，$T_{\mathrm{e2e}}$、TTFT（time to first token，首个 token 生成时延）和 decode throughput（解码吞吐率，token/s，即每秒生成 token 数）可由设定的利用率、带宽和任务约束估算。

令 $E_{\mathrm{task}}$ 为单次任务能耗，$P_{\mathrm{avg}}$ 为任务执行期间的平均功耗，则任务能耗可由设定的平均功耗估计：

$$
E_{\mathrm{task}}=P_{\mathrm{avg}}T_{\mathrm{e2e}}.
$$

“任务可部署”要求以下条件同时成立：$T_{\mathrm{deadline}}$ 为任务允许的最大端到端时延，$E_{\mathrm{budget}}$ 为单次任务的能量预算，$P_{\mathrm{peak}}$ 为任务运行期间的峰值功耗，$P_{\mathrm{thermal}}$ 为热设计或热控允许的最大功耗：

$$
\begin{cases}
M_{\mathrm{req}}\le M_{\mathrm{usable}},\\
T_{\mathrm{e2e}}\le T_{\mathrm{deadline}},\\
E_{\mathrm{task}}\le E_{\mathrm{budget}},\\
P_{\mathrm{peak}}\le P_{\mathrm{thermal}}.
\end{cases}
$$

---

## 3. Qwen3-VL-4B 示例

本节将前述通用流程具体化为 Qwen3-VL-4B 的单板推理示例。模型结构参数采用其 `config.json` 对应的文本解码器配置；参数量根据公开 safetensors 权重文件头统计：语言侧约 $4.022$B、视觉编码器与 projector 合计约 $0.415$B。输入长度和量化配置为本节固定假设；Qwen3-VL-4B 属于 3B–4B VLM 档，辅助内存取 $M_{\mathrm{aux}}=1.0$ GiB。

### 3.1 固定配置

| 项目 | 取值 |
|---|---:|
| 语言主干参数量 $P_{\mathrm{LLM}}$ | $4.022$B |
| 视觉编码器与 projector 参数量 $P_{\mathrm{vis}}+P_{\mathrm{proj}}$ | $0.415$B |
| 完整 VLM 总参数量 $P_{\mathrm{total}}$ | 约 $4.438$B |
| Transformer 层数 $L$ | $36$ |
| hidden size $H$ | $2560$ |
| query heads $n_{\mathrm{q}}$ | $32$ |
| KV heads $n_{\mathrm{kv}}$ | $8$ |
| head dimension $d_h$ | $128$ |
| attention query 投影维度 $D_Q=n_{\mathrm{q}}d_h$ | $4096$ |
| batch size $B$ | $1$ |
| 文本输入 $N_{\mathrm{text}}$ | $1024$ |
| 视觉 token $N_{\mathrm{vis}}$ | $1024$ |
| 最大输出长度 $N_{\mathrm{out}}$ | $1024$ |
| KV cache 精度 | BF16，$s_{\mathrm{KV}}=2$ Byte |
| 视觉编码计算量 $F_{\mathrm{vision}}$ | $0.05$ TFLOPs（示例假设） |
| 辅助内存 $M_{\mathrm{aux}}$ | $1.0$ GiB |

因此：

$$
T_{\mathrm{in}}=N_{\mathrm{text}}+N_{\mathrm{vis}}=1024+1024=2048,
$$

$$
T_{\mathrm{cap}}=T_{\mathrm{in}}+N_{\mathrm{out}}=2048+1024=3072.
$$

### 3.2 W4 权重内存

语言主干采用 W4，取有效平均位宽 $b_{\mathrm{LLM}}^{\mathrm{eff}}=4.25$ bit；视觉编码器与 projector 保持 BF16。

语言主干权重内存记为 $M_{\mathrm{LLM}}$：

$$
M_{\mathrm{LLM}}
=
\frac{4.022468096\times10^9\times4.25}{8\times2^{30}}
=1.990\ \mathrm{GiB}.
$$

视觉编码器与投影器权重内存之和记为 $M_{\mathrm{vis+proj}}$：

$$
M_{\mathrm{vis+proj}}
=
\frac{0.415347712\times10^9\times16}{8\times2^{30}}
=0.774\ \mathrm{GiB}.
$$

故：

$$
M_{\mathrm{weight}}
=1.990+0.774
=2.764\ \mathrm{GiB}.
$$

### 3.3 KV cache 内存

每 token 的 KV cache 成本为：

$$
c_{\mathrm{KV}}
=
\frac{2\times1\times36\times8\times128\times2}{2^{30}}
=1.373\times10^{-4}\ \mathrm{GiB/token}.
$$

即：

$$
c_{\mathrm{KV}}=144\ \mathrm{KiB/token}.
$$

因此：

$$
M_{\mathrm{KV}}
=3072\times144\ \mathrm{KiB}
=0.422\ \mathrm{GiB}.
$$

### 3.4 峰值内存需求

模型侧基础内存为：

$$
M_{\mathrm{base}}
=M_{\mathrm{weight}}+M_{\mathrm{KV}}
=2.764+0.422
=3.186\ \mathrm{GiB}.
$$

按模型规模固定取值：

$$
M_{\mathrm{aux}}
=1.0\ \mathrm{GiB}.
$$

因此：

$$
M_{\mathrm{req}}
=M_{\mathrm{base}}+M_{\mathrm{aux}}
=3.186+1.000
=4.186\ \mathrm{GiB}.
$$

其中 $M_{\mathrm{aux}}$ 为模型规模固定估计。

### 3.5 不同权重量化配置比较

保持相同输入长度、KV cache 精度与 $M_{\mathrm{aux}}=1.0$ GiB 假设：

| 语言主干精度           | $M_{\mathrm{weight}}$ | $M_{\mathrm{KV}}$ | $M_{\mathrm{base}}$ | $M_{\mathrm{aux}}$ | $M_{\mathrm{req}}$ |
| ---------------- | --------------------: | ----------------: | ------------------: | -----------------: | -----------------: |
| BF16             |             8.266 GiB |         0.422 GiB |           8.688 GiB |          1.000 GiB |          9.688 GiB |
| W8，有效 $8.5$ bit  |             4.754 GiB |         0.422 GiB |           5.176 GiB |          1.000 GiB |          6.176 GiB |
| W4，有效 $4.25$ bit |             2.764 GiB |         0.422 GiB |           3.186 GiB |          1.000 GiB |          4.186 GiB |

### 3.6 板卡容量判定示例

以可用内存 $6.8$、$13.6$ 和 $27.2$ GiB 的三类示例板卡为例：

| 示例板卡 | $M_{\mathrm{usable}}$ | BF16 | W8 | W4 |
|---|---:|---|---|---|
| A | 6.8 GiB | ✕ | ✓ | ✓ |
| B | 13.6 GiB | ✓ | ✓ | ✓ |
| C | 27.2 GiB | ✓ | ✓ | ✓ |

以板卡 A 的 W4 组合为例：

$$
\Delta M
=6.8-4.186
=2.614\ \mathrm{GiB}>0.
$$

该组合通过内存门槛。对 W8 权重量化配置，将其内存裕量记为 $\Delta M_{\mathrm{W8}}$：

$$
\Delta M_{
\mathrm{W8}}
=6.8-6.176
=0.624\ \mathrm{GiB},
$$

因此记为内存可部署。

### 3.7 计算量近似

由于该模型的 attention query 投影维度为：

$$
D_Q=n_{\mathrm{q}}d_h=32\times128=4096,
$$

prefill 计算量为：

$$
\begin{aligned}
F_{\mathrm{prefill}}
&\approx F_{\mathrm{vision}}
+2P_{\mathrm{LLM}}T_{\mathrm{in}}
+4LD_QT_{\mathrm{in}}^2\\
&=0.05+16.476+2.474\\
&=19.000\ \mathrm{TFLOPs}.
\end{aligned}
$$

生成 $1024$ 个 token 的 decode 计算量为：

$$
\begin{aligned}
F_{\mathrm{decode}}
&\approx2P_{\mathrm{LLM}}N_{\mathrm{out}}
+4LD_Q\left[N_{\mathrm{out}}T_{\mathrm{in}}
+\frac{N_{\mathrm{out}}(N_{\mathrm{out}}-1)}{2}\right]\\
&=8.238+1.546\\
&=9.784\ \mathrm{TFLOPs}.
\end{aligned}
$$

因此：

$$
F_{\mathrm{task}}
=F_{\mathrm{prefill}}+F_{\mathrm{decode}}
=28.784\ \mathrm{TFLOPs}.
$$

该 FLOPs 结果用于比较板卡上的理论计算压力。
## 4. 板卡信息

除 Jetson SoM/开发套件外，本表以单张加速卡或单个 GPU/OAM/SXM 模块为单位；不纳入系统级基板、多卡服务器，以及必须通过多芯模型并行才能使用聚合内存的双芯卡。

| 硬件                                                | 类别与形态                                     |                          $M_{\mathrm{phys}}$ |                  $B_{\mathrm{mem}}$ | $C_{\mathrm{peak}}$（厂商标称）                            | $P_{\mathrm{max}}$ |
| ------------------------------------------------- | ----------------------------------------- | -------------------------------------------: | ----------------------------------: | ---------------------------------------------------- | -----------------: |
| NVIDIA Jetson Orin Nano 4GB                       | 低功耗边缘 SoM；统一内存                            |                                  4 GB LPDDR5 |                             51 GB/s | 34 TOPS                                              |      7 W / 10 W / 25 W |
| NVIDIA Jetson Orin Nano Super Developer Kit       | 低功耗边缘开发套件；统一内存                            |                                  8 GB LPDDR5 |                            102 GB/s | 67 TOPS                                              |               25 W |
| NVIDIA Jetson Orin NX 16GB                        | 边缘 SoM（System-on-Module，系统级模块）            |                                 16 GB LPDDR5 |                          102.4 GB/s | 157 TOPS                                             |               40 W |
| NVIDIA Jetson AGX Orin 64GB                       | 高性能边缘 SoM/开发套件；统一内存                       |                                 64 GB LPDDR5 |                          204.8 GB/s | 275 TOPS                                             |               60 W |
| NVIDIA Jetson AGX Thor（Jetson T5000）              | 新一代高端边缘开发套件；统一内存                          |                               128 GB LPDDR5X |                            273 GB/s | 2070 FP4 TFLOPS（稀疏）                                  |              130 W |
| NVIDIA GeForce RTX 4090 Founders Edition          | 单 GPU 工作站卡                                |                                 24 GB GDDR6X |                           1008 GB/s | 83 TFLOPS（FP32）；1321 AI TOPS                         |              450 W |
| NVIDIA RTX PRO 6000 Blackwell Workstation Edition | 单 GPU 专业工作站卡                              | 96 GB GDDR7，带 ECC（Error-Correcting Code，纠错码） |                           1792 GB/s | 4000 AI TOPS（FP4、稀疏）；125 TFLOPS（FP32）                |              600 W |
| NVIDIA H100 SXM                                   | 数据中心 GPU 模块                               |                                   80 GB HBM3 |                           3.35 TB/s | 1979 BF16 TFLOPS（稀疏）                                 |              700 W |
| NVIDIA B200 SXM6                                  | 单 GPU 数据中心模块；第五代 NVLink                   |                        180 GB HBM3e；单 GPU 资源 |                              8 TB/s | 4.5 PFLOPS（BF16/FP16 Tensor Core、稀疏）；2.25 PFLOPS（稠密） |           约 1000 W |
| AMD Instinct MI300X                               | 数据中心 OAM（Open Accelerator Module，开放加速器模块） |                                  192 GB HBM3 |                            5.3 TB/s | 1.3 BF16 PFLOPS；2.6 FP8 PFLOPS                       |              750 W |
| 华为 Atlas 300I Pro                                 | 单卡边缘/数据中心 PCIe 推理卡；半高半长 PCIe Gen4 ×16     |                                24 GB LPDDR4X |                          204.8 GB/s | 140 TOPS（INT8）；70 TFLOPS（FP16）                       |           72 W（最大） |
| 华为 Atlas 300I A2                                  | 双槽位全高全长 PCIe 推理卡                          |             32 GB 片上内存（ECC）或 64 GB 片上内存（ECC） | 32 GB 配置：0.8 TB/s；64 GB 配置：1.6 TB/s | 560 TOPS（INT8）；280 TFLOPS（FP16）                      |          350 W（最大） |
| 寒武纪 MLU370-X4                                     | 单加速器数据中心/边缘推理卡；PCIe Gen4 ×16              |                                 24 GB LPDDR5 |                          307.2 GB/s | 96 TFLOPS（FP16/BF16）；256 TOPS（INT8）；24 TFLOPS（FP32）  |          150 W（最大） |

- NVIDIA Jetson Orin: <https://www.nvidia.com/en-us/autonomous-machines/embedded-systems/jetson-orin/>
- NVIDIA Jetson Thor: <https://www.nvidia.com/en-us/autonomous-machines/embedded-systems/jetson-thor/>
- NVIDIA GeForce RTX 4090: <https://www.nvidia.com/en-us/geforce/graphics-cards/40-series/rtx-4090/>
- NVIDIA RTX PRO 6000 Blackwell: <https://www.nvidia.com/en-us/products/workstations/professional-desktop-gpus/rtx-pro-6000-family/>
- NVIDIA H100: <https://www.nvidia.com/en-us/data-center/h100/>
- NVIDIA B200 SXM6（单 GPU 180 GB HBM3e、最高可配置至 1000 W）: <https://images.nvidia.com/aem-dam/Solutions/documents/HGX-B200-PCF-Summary.pdf>
- AMD Instinct MI300X: <https://www.amd.com/en/products/accelerators/instinct/mi300/mi300x.html>
- 华为 Atlas 300I Pro 产品规格: <https://e.huawei.com/cn/products/computing/ascend/atlas-300-ai>
- 华为 Atlas 300I A2 产品规格: <https://e.huawei.com/cn/products/computing/ascend/atlas-300i-a2>
- 寒武纪 MLU370-X4 产品规格: <https://www.cambricon.com/index.php?a=lists&c=index&catid=371&m=content>
- 面向 LPDDR 类加速器的 LLM serving 内存管理研究: <https://arxiv.org/abs/2512.09427>
- Atlas A2 低比特推理研究: <https://arxiv.org/abs/2512.23367>

## 5. 表 1 内容

### 不同模型规模在具体板卡上的可部署情况

| 模型             | $M_{\mathrm{req}}$ | Orin Nano 4GB | Orin Nano Super | Orin NX 16GB | AGX Orin 64GB | AGX Thor 128GB | RTX 4090 | RTX PRO 6000 | H100 SXM | B200 SXM6 | MI300X | Atlas 300I Pro | Atlas 300I A2 32GB | MLU370-X4 |
| -------------- | -----------------: | ------------- | --------------- | ------------ | ------------- | -------------- | -------- | ------------ | -------- | --------- | ------ | -------------- | ------------------ | --------- |
| InternVL3.5-1B |          1.776 GiB | ✓             | ✓               | ✓            | ✓             | ✓              | ✓        | ✓            | ✓        | ✓         | ✓      | ✓              | ✓                  | ✓         |
| Qwen3-VL-2B    |          2.437 GiB | ✓             | ✓               | ✓            | ✓             | ✓              | ✓        | ✓            | ✓        | ✓         | ✓      | ✓              | ✓                  | ✓         |
| Qwen2.5-VL-3B  |          3.878 GiB | ✕             | ✓               | ✓            | ✓             | ✓              | ✓        | ✓            | ✓        | ✓         | ✓      | ✓              | ✓                  | ✓         |
| Qwen3-VL-4B    |          4.186 GiB | ✕             | ✓               | ✓            | ✓             | ✓              | ✓        | ✓            | ✓        | ✓         | ✓      | ✓              | ✓                  | ✓         |
| Qwen2.5-VL-7B  |          6.692 GiB | ✕             | ○               | ✓            | ✓             | ✓              | ✓        | ✓            | ✓        | ✓         | ✓      | ✓              | ✓                  | ✓         |
| Qwen3-VL-8B    |          7.048 GiB | ✕             | ○               | ✓            | ✓             | ✓              | ✓        | ✓            | ✓        | ✓         | ✓      | ✓              | ✓                  | ✓         |
| Qwen3-VL-32B   |         21.068 GiB | ✕             | ✕               | ✕            | ✓             | ✓              | ○        | ✓            | ✓        | ✓         | ✓      | ○              | ✓                  | ○         |
| EarthDial-4B   |          4.621 GiB | ✕             | ✓               | ✓            | ✓             | ✓              | ✓        | ✓            | ✓        | ✓         | ✓      | ✓              | ✓                  | ✓         |
| EarthMind-4B   |          4.333 GiB | ✕             | ✓               | ✓            | ✓             | ✓              | ✓        | ✓            | ✓        | ✓         | ✓      | ✓              | ✓                  | ✓         |
| GeoChat-7B     |          6.938 GiB | ✕             | ○               | ✓            | ✓             | ✓              | ✓        | ✓            | ✓        | ✓         | ✓      | ✓              | ✓                  | ✓         |

以上均为 VLM Instruct 模型；权重量化 / KV cache 精度设定为统一 W4 / BF16，不代表模型官方权重的默认存储格式。表 1A 仅计算内存可部署性，$M_{\mathrm{req}}=M_{\mathrm{base}}+M_{\mathrm{aux}}$；符号含义为：✓ = 内存可部署，○ = 边界可部署，✕ = 不可部署。边界可部署表示 $\Delta M\ge 0$，但剩余内存小于板卡可用内存的 10%，即 $0\le \Delta M/M_{\mathrm{usable}}<10\%$。实际部署还需考虑推理后端、端到端时延、功耗和热约束，但这些因素不进入本表计算。

- InternVL3.5-1B-Instruct: <https://huggingface.co/OpenGVLab/InternVL3_5-1B-Instruct>
- Qwen3-VL-2B/4B/8B/32B-Instruct: <https://huggingface.co/Qwen/Qwen3-VL-2B-Instruct>、<https://huggingface.co/Qwen/Qwen3-VL-4B-Instruct>、<https://huggingface.co/Qwen/Qwen3-VL-8B-Instruct>、<https://huggingface.co/Qwen/Qwen3-VL-32B-Instruct>
- Qwen2.5-VL-3B-Instruct / Qwen2.5-VL-7B-Instruct: <https://huggingface.co/Qwen/Qwen2.5-VL-3B-Instruct>、<https://huggingface.co/Qwen/Qwen2.5-VL-7B-Instruct>
- GeoChat-7B: <https://huggingface.co/MBZUAI/geochat-7B>、<https://github.com/mbzuai-oryx/GeoChat>
- EarthDial-4B: <https://huggingface.co/akshaydudhane/EarthDial_4B_RGB>、<https://github.com/hiyamdebary/EarthDial>
- EarthMind-4B: <https://arxiv.org/abs/2506.01667>

### 计算明细

权重量化 / KV cache 精度设定为统一 W4 / BF16。$N_{\mathrm{vis}}$ 设定为 1024，表示进入语言模型的视觉 token 数固定为 1024 的归一化压力测试，不代表各模型处理同一原始图像分辨率时的实际显存占用。$M_{\mathrm{aux}}$ 不重复计入操作系统、驱动和其他已从 $M_{\mathrm{usable}}$ 中扣除的保留内存，只作为分析性假设估计模型执行过程中的视觉激活、workspace、运行时内存池和碎片余量。

| 模型             | $P_{\mathrm{total}}$ | $M_{\mathrm{weight}}$ | $M_{\mathrm{KV}}$ | $M_{\mathrm{base}}$ | $M_{\mathrm{aux}}$ | $M_{\mathrm{req}}$ |
| -------------- | -------------------: | --------------------: | ----------------: | ------------------: | -----------------: | -----------------: |
| InternVL3.5-1B |              1.061 B |             0.948 GiB |         0.328 GiB |           1.276 GiB |          0.500 GiB |          1.776 GiB |
| Qwen3-VL-2B    |              2.128 B |             1.609 GiB |         0.328 GiB |           1.937 GiB |          0.500 GiB |          2.437 GiB |
| Qwen2.5-VL-3B  |              3.755 B |             2.772 GiB |         0.105 GiB |           2.878 GiB |          1.000 GiB |          3.878 GiB |
| Qwen3-VL-4B    |              4.438 B |             2.764 GiB |         0.422 GiB |           3.186 GiB |          1.000 GiB |          4.186 GiB |
| Qwen2.5-VL-7B  |              8.292 B |             5.028 GiB |         0.164 GiB |           5.192 GiB |          1.500 GiB |          6.692 GiB |
| Qwen3-VL-8B    |              8.767 B |             5.126 GiB |         0.422 GiB |           5.548 GiB |          1.500 GiB |          7.048 GiB |
| Qwen3-VL-32B   |             33.357 B |            17.318 GiB |         0.750 GiB |          18.068 GiB |          3.000 GiB |         21.068 GiB |
| EarthDial-4B   |              4.146 B |             2.496 GiB |         1.125 GiB |           3.621 GiB |          1.000 GiB |          4.621 GiB |
| EarthMind-4B   |              4.000 B |             3.228 GiB |         0.105 GiB |           3.333 GiB |          1.000 GiB |          4.333 GiB |
| GeoChat-7B     |              7.063 B |             3.938 GiB |         1.500 GiB |           5.438 GiB |          1.500 GiB |          6.938 GiB |
